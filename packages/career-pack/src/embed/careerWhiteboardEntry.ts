/**
 * Combined career whiteboard embed bundle entry — single build graph for
 * `<agentable-whiteboard>` + career-pack provider (no duplicate tldraw).
 */
import './careerWhiteboardProviderBootstrap';
import '../../../../src/embed/agentable-whiteboard';
