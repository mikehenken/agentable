import type { CareerDataset } from '../types';
import raw from './sandals.json';

/** Schema-validated sandals career fixture (career-canvas demo data). */
export const SANDALS_CAREER_DATASET: CareerDataset = raw as CareerDataset;
