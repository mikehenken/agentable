# Changesets

This folder tracks pending version bumps for `agentable-canvas`. Add a markdown file here
when shipping user-facing API or behavior changes. At publish time, `@changesets/cli`
consumes these files to update `CHANGELOG.md` and `package.json`.

See https://github.com/changesets/changesets/blob/main/docs/adding-a-changeset.md
