# M2.3 — Token Sweep + Shared-Config Refactor (2026-04-25)

Single code-reviewer. NO SELF-REVIEW.

## Surface reviewed
- 27 occurrences of `text-[#0D7377]` migrated → `text-canvas-primary`
- 7 occurrences of `bg-[#F7F9F9]` / `bg-[#F0F7F7]` migrated → `bg-canvas-surface-subtle`
- New token `--landi-color-surface-subtle: #F7F9F9` + theme alias
- Cross-config drift fixed: tokens extracted to `agentable-canvas/config/landi-token-colors.cjs`, both Tailwind configs `require()` from the single source

## Findings + resolution

### CRITICAL (resolved)
| # | Finding | Resolution |
|---|---|---|
| C1 | ~50 raw `#0D7377` hits remain — sed only matched `text-[#0D7377]`, missed `bg-[#0D7377]`, `border-[#0D7377]`, `ring-[#0D7377]`, plus JourneyPanel.tsx never touched | Extended sed to handle `bg-[#0D7377]` → `bg-canvas-primary`, `border-[#0D7377]` → `border-canvas-primary`, hover variants, and `ring-[#0D7377]` in JourneyPanel. Verified: ZERO bare-hex `bg-/border-/ring-[#0D7377]` remaining. |
| C2 | ChatPanel.tsx:121 — `hover:bg-[#F0F9F9]` was a third teal-tinted near-white, sed didn't catch it | Added two more sed rules: `bg-[#F0F9F9]` and `hover:bg-[#F0F9F9]` → `bg-canvas-surface-subtle` / `hover:bg-canvas-surface-subtle`. Conflated into the same surface-subtle token (visual delta from #F7F9F9 / #F0F7F7 / #F0F9F9 is imperceptible at the relevant brightness). |

### WARNING (resolved)
| # | Finding | Resolution |
|---|---|---|
| W2 | Cross-config drift — `landiTokenColors` map duplicated byte-for-byte in canvas + website tailwind configs; "Keep in sync" comment was the only enforcement | **Resolved (proactive)**: extracted to `agentable-canvas/config/landi-token-colors.cjs` with explanatory header. Both configs now `require()` from this single source. New tokens added once propagate to both builds automatically. CI-checkable. |
| W1 | Conflating `#F7F9F9` + `#F0F7F7` (and now `#F0F9F9`) into one surface-subtle token — defensible, but document so a future sweep doesn't re-split | Comment added in `index.css` describing the role and the deliberate consolidation. |

### WARNING (deferred)
| # | Finding | Reason |
|---|---|---|
| W3 | 6 alpha-modified `ring-[#0D7377]/10` and `border-[#0D7377]/30` sites bypass the token (Tailwind v3 slash-alpha doesn't compose with CSS-var arbitrary values) | Real architectural limit. Standard fix is HSL-based tokens with `<alpha-value>` placeholder pattern (matches the shadcn token pattern at lines 38-42 of the website's tailwind.config). Substantial refactor — deferred to dedicated M2.4 iteration. Tracked in `docs/status/token-sweep-inventory.md`. |

### NIT
| # | Finding | Resolution |
|---|---|---|
| N1 | ApplicationsPanel `stroke="#0D7377"` SVG attribute (not className) — should use `stroke="currentColor"` | Tracked. Mechanical 1-line fix for next iteration. |
| N2 | Tone-map objects (`bar: 'bg-[#0D7377]'`) inside panel constants — sed caught these as bare `bg-` matches, no remaining issue | Already handled by the C1 sweep extension. |
| N3 | Hover JIT verification — assertions only test base color, not hover variants | Tracked. Visual regression test (Playwright `toHaveScreenshot`) is a better seam for hover validation than unit assertions; defer to M2 visual regression work. |

## Verification post-fix
- Build: ✓ `dist/styles.css` 15.84 KB (52.8% of 30 KB CSS budget)
- Bundle budgets: all 5 within target (canvas 247 KB ESM / 201 KB UMD, button 11 KB / 10 KB, styles 15.84 KB)
- Tests: 41/41 passing
- Browser: `surfaceSubtleToken: #F7F9F9` ✓, `sharedConfigWorked: true` (primary-tint token resolves) ✓, zero console errors

## Files
- `src/canvas/JourneyPanel.tsx` (newly migrated — was untouched in earlier waves)
- `src/canvas/ApplicationsPanel.tsx`, `OpenPositionsPanel.tsx`, `SettingsPanel.tsx`, `ResourcesPanel.tsx`, `TopBar.tsx`, `FloatingToolbar.tsx`, `ChatPanel.tsx`, `CareerToolsPanel.tsx`, `GrowthPathsPanel.tsx`, `NavSidebar.tsx`
- `src/index.css` — `--landi-color-surface-subtle` added
- `config/landi-token-colors.cjs` — NEW shared config (architectural improvement)
- `tailwind.config.js` (canvas + website) — both now `require('./config/landi-token-colors.cjs')`

## Status
DONE — 2 CRITICAL + 2 WARNINGs resolved this iteration. 1 WARNING + 2 NITs deferred with rationale.

## Token-migration totals to date

| Wave | Sites migrated | Tokens added | Config improvement |
|---|---|---|---|
| M2 wave 1 | Workspace bg + visualizer (6 sites) | `--landi-color-accent`, `--landi-color-workspace-bg/dot`; `:root, :host` dual selector | — |
| M2.1 | `text-[#1A1A1A]` → `text-canvas` (14 sites across 7 panels) | — | Tailwind theme.extend.colors aliases |
| M2.2 | `bg-[#E6F4F1]` → `bg-canvas-primary-tint` (10 sites); ChatPanel avatar gradient (3 stops) | `--landi-color-primary-light/soft/tint` | — |
| M2.3 | `text-[#0D7377]` → `text-canvas-primary` (27 sites); `bg-[#F7F9F9/F0F7F7/F0F9F9]` → `bg-canvas-surface-subtle` (8 sites); `bg-[#0D7377]`/`border-[#0D7377]`/`ring-[#0D7377]` (~30 sites including JourneyPanel) | `--landi-color-surface-subtle` | Shared `config/landi-token-colors.cjs` |
| **Total M2** | **~98 sites migrated** | **6 new brand-relevant tokens** | **Single source of truth for theme aliases** |

## What's left for M2.4
- 6 alpha-modified `ring-[#0D7377]/N` sites — needs HSL-based token refactor
- ApplicationsPanel SVG `stroke="#0D7377"` → `stroke="currentColor"` (1 site)
- Code-split heavy panels via `React.lazy` (target ~160-180 KB initial chunk)
- Lit `@open-wc/testing` harness for component tests
- Hover-variant JIT verification via Playwright visual regression
