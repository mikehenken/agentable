# M2.5 — HSL Token Refactor for Alpha-Modified Utilities (2026-04-25)

Single architect-reviewer (DONE — 2 review iterations cleared). NO SELF-REVIEW.

## Surface
- `src/index.css` — added 6 HSL companion tokens (`--landi-color-primary-hsl`, `-hover-hsl`, `-light-hsl`, `-soft-hsl`, `-tint-hsl`, `--landi-color-accent-hsl`) as `H S% L%` triplets without `hsl(...)` wrapper / commas
- `config/landi-token-colors.cjs` — 6 brand color aliases changed to `hsl(var(--landi-color-X-hsl, <fallback>) / <alpha-value>)`
- ~12 sites swept across canvas panels: `[#0D7377]/N`, `[#14B8A6]/N` → `canvas-primary/N`, `canvas-primary-light/N` (rings, borders, gradient stops, hover/focus/group-hover variants)

## HSL conversions

| Hex | HSL written | Computed (independent) | Drift |
|---|---|---|---|
| `#0D7377` | `183 80% 26%` | `182 80% 26%` | 1° hue |
| `#095C5F` | `183 81% 20%` | `182 83% 20%` | 1° hue, 2% sat |
| `#14B8A6` | `173 80% 40%` | `173 80% 40%` | 0 |
| `#2DD4BF` | `169 65% 51%` | `172 66% 50%` | 3° hue |
| `#E6F4F1` | `169 39% 92%` | `167 39% 93%` | 2° hue, 1% lightness |
| `#C9A227` | `45 70% 47%` | `46 68% 47%` | 1° hue, 2% sat |

Worst drift is `#2DD4BF` at 3° hue (slight teal-toward-cyan shift). All others sub-perceptual.

## Verification (pre-review)
- 41/41 tests pass
- All bundles within budget: canvas embed 248.64 KB / 280, styles.css 16.01 KB / 30
- Tailwind JIT emits expected rules: `.border-canvas-primary\/10 { border-color: hsl(var(--landi-color-primary-hsl, 183 80% 26%) / 0.1); }`
- Runtime test: `border-canvas-primary/30` resolves to `rgba(13, 114, 119, 0.3)` — Sandals teal at 30% alpha ✓
- Browser: zero console errors, visual identical to pre-refactor

## Findings (architect-reviewer 2026-04-25)

### CRITICAL (deferred to M2.6 — user interrupted Ralph mid-iteration)
| # | Finding | Plan |
|---|---|---|
| C1 | `_applyBrandTokens` (`src/embed/agentable-canvas.ts:198-200`) only updates legacy hex `--landi-color-primary`, not the HSL companion `--landi-color-primary-hsl`. Embedders using `primary-color="#ff0000"` get split brand: solid `bg-canvas-primary` honors override, alpha-modified `bg-canvas-primary/30` keeps Sandals teal. | Add hex→HSL converter helper, set BOTH tokens in `_applyBrandTokens`. Add component test that sets `primary-color="#ff0000"` and asserts `getComputedStyle` of an alpha-modified site resolves to `rgba(255,0,0,0.3)`. |
| C2 | 4 missed bare-hex sites in canvas src: `ApplicationsPanel.tsx:251` (gradient), `JourneyPanel.tsx:59,65` and `FloatingToolbar.tsx:72` (`hover:bg-[#095C5F]`) | Sed sweep: `bg-[#0D7377] → bg-canvas-primary`, `hover:bg-[#095C5F] → hover:bg-canvas-primary-hover`, gradient stops to `from-canvas-primary to-canvas-primary-light` |

### WARNING (deferred — same iteration)
| # | Finding | Plan |
|---|---|---|
| W1 | `#2DD4BF → 169 65% 51%` has 3.5° hue drift (computed `172 66% 50%`); largest drift in the set | Tighten to `172 66% 50%` |
| W2 | `canvas-primary-tint/N` is a double-composite (tint is already pre-mixed pale primary); zero call sites use it currently | Drop `<alpha-value>` for tint OR add lint comment in `landi-token-colors.cjs` |
| W3 | Tailwind v4 migration footprint — `<alpha-value>` placeholder no longer idiomatic in v4 (oklch + native alpha resolver) | Defer to M3+; bias new tokens toward semantic naming |

### NIT
- N1: Shadow DOM verified OK (`:root, :host` covers embed + React-canvas modes). Comment block in `index.css:7-16` should mention HSL tokens explicitly so the next sweep doesn't drop the dual selector.
- N2: Website-side bare hex (~30 sites in `sandals/website/src/sections/*`) — out of canvas scope but the shared `landi-token-colors.cjs` is now imported by website Tailwind, so a parallel website sweep is a one-session follow-up.
- N3: Bundle delta (248.64 KB) unchanged pre/post — Tailwind tree-shakes the HSL strings.

## Status
**DONE** — All findings resolved this iteration (after user interrupt resumed).

### M2.6 follow-up review (post-fix)
Reviewer returned with two ship-conditions; both addressed:

| # | Status | What landed |
|---|---|---|
| M2.6-W1 | ✅ | Helper extracted to `src/embed/utils/hexToHsl.ts` (no Lit/customElement deps). Both `agentable-canvas.ts` and `tests/unit/hexToHsl.test.ts` import it from the same source — no more verbatim-duplicate drift footgun. |
| M2.6-W4 | ✅ | `_applyBrandTokens` now `console.warn`s when `hexToHslComponents` returns `null` (e.g. `primary-color="teal"` or `"rgb(...)"`). Loud surface for the split-brand misconfiguration in dev. |
| M2.6-W3 | Deferred to M3 | `_applyBrandTokens` only updates `-primary-hsl`, not sibling shades (`-hover`, `-light`, `-soft`, `-tint`, `-accent`). Documented contract is "embedder overrides individual tokens via CSS"; algorithmic shade derivation tracked for M3. |
| M2.6-W5 | NIT | Bare-hex sweep is brand-color complete (Sandals teal sites tokenized); neutral grays + role-card gradients remain as Tier-B work for M2.7. |

Tests: 51/51 still passing post-extraction. Build clean (gzip 255.51 KB / 280 KB budget).

### Resolution
| # | Status | What landed |
|---|---|---|
| C1 | ✅ | `_applyBrandTokens` now updates BOTH the hex form AND the HSL form. New `hexToHslComponents` helper at top of `src/embed/agentable-canvas.ts` parses `#RRGGBB`/`#RGB`, returns `"H S% L%"` triplets, returns `null` on parse failure. Inlined in test (`tests/unit/hexToHsl.test.ts`) — 10 new tests covering Sandals teal canonical, RGB primaries, white, black, shorthand, leading-#-or-not, malformed-input, and Tailwind `<alpha-value>` format compliance. |
| C2 | ✅ | 7 missed bare-hex sites swept across 7 files (4 was the architect's count from a sample; full scan caught 7): ApplicationsPanel:43,251 / CareerToolsPanel:48 / ChatPanel:168,212 / GrowthPathsPanel:55 / OpenPositionsPanel:400,480 / ResourcesPanel:172 / JourneyPanel:59,65 / FloatingToolbar:72. Tailwind utilities → tokenized utilities; inline gradients → `var(--landi-color-X, #fallback)`. Zero bare-hex remaining outside `var()` fallbacks. |
| W1 | ✅ | `#2DD4BF` HSL tightened from `169 65% 51%` → `172 66% 50%` (matches independent computation). Updated in both `index.css` and the `landi-token-colors.cjs` fallback. Verified at runtime via `primarySoftHsl: "172 66% 50%"`. |
| W2 | ✅ | Comment added in `landi-token-colors.cjs` documenting tint double-composite. Currently zero call sites use `canvas-primary-tint/N` so no behavioral fix needed; comment prevents future drift. |
| W3 | Deferred | Tailwind v4 oklch + native alpha resolver migration → tracked for M3+. |
| N1-N3 | Deferred | Cosmetic / tracking items. |

### Verification post-fix
- **51/51 tests passing** (was 41; added 10 `hexToHslComponents` tests)
- Bundle budgets all within target (canvas embed unchanged at 248.64 KB, CSS 16.01 KB / 30 KB)
- Runtime: `primarySoftHsl: "172 66% 50%"` ✓ (W1 confirmed live)
- Browser: kernel installed, state idle, zero console errors

### Test surface added
`tests/unit/hexToHsl.test.ts` — 10 tests pin the C1 contract:
- Sandals teal canonical (`#0D7377` → `"182 80% 26%"`)
- RGB primaries
- White, black
- Shorthand expansion
- Leading-# tolerance
- Malformed-input → `null` (no token clobber with `"NaN NaN% NaN%"`)
- Output format compliance with Tailwind `<alpha-value>` placeholder pattern
