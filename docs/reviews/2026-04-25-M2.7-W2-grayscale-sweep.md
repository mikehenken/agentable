# M2.7 W2 / Track F.7.2 — Tier-B Grayscale Token Sweep (2026-04-25)

Single architect-reviewer pass + same-iter resolution. NO SELF-REVIEW.

## Surface

- 15 panel files swept: `BottomBar`, `CareerToolsPanel`, `ArtifactsPanel`, `DraggablePanel`, `ChatPanel`, `FloatingToolbar`, `GrowthPathsPanel`, `ApplicationsPanel`, `NavSidebar`, `JourneyPanel`, `OpenPositionsPanel`, `ResourcesPanel`, `VoiceWidget`, `TopBar`, `SettingsPanel`.
- 241 sites migrated per the canonical mapping rules.
- New token alias `canvas-border` → `var(--landi-color-border, #E5E5E5)` added to shared `agentable-canvas/config/landi-token-colors.cjs`.

## Mapping

```
text-gray-900/800           → text-canvas
text-gray-700/600           → text-canvas-muted
text-gray-500/400/300       → text-canvas-faint
hover:text-gray-900         → hover:text-canvas
hover:text-gray-700/600     → hover:text-canvas-muted
bg-white                    → bg-canvas-surface
bg-gray-50/100              → bg-canvas-surface-subtle
hover:bg-gray-50/100        → hover:bg-canvas-surface-subtle
border-gray-100/200/300     → border-canvas-border
```

## Findings (architect-reviewer 2026-04-25)

### CRITICAL — RESOLVED THIS ITERATION

| # | Finding | Resolution |
|---|---|---|
| C1 | `ChatPanel.tsx:183` and `:199` left `hover:bg-gray-200` orphaned (icon buttons with transparent base; hover is the only rendered surface — split-brand on dark theme) | Both swept to `hover:bg-canvas-surface-subtle`. Rule extension: hover-only fills follow the same tokenize logic as base hovers. |

### WARNING — RESOLVED THIS ITERATION

| # | Finding | Resolution |
|---|---|---|
| W1 | 7 `bg-gray-200` static dividers / tracks (BottomBar:11,15,19 / FloatingToolbar:105 / JourneyPanel:24 / ApplicationsPanel:164,229 / SettingsPanel:111) leak brand on dark theme | All 7 swept to `bg-canvas-border`. Toggle off-state and progress empty-track use the same border token — defensible given they're visually identical to dividers (1-px / `h-0.5` / `h-1` filled gray). If a future tenant needs distinct semantics, introduce `canvas-track` and split call sites. |
| W2 | `--landi-color-text-secondary: #757575` was a 24-point lightness jump from old `text-gray-700` `#374151`. Small-text WCAG dropped 10.3:1 → 4.6:1 (still AA but barely AA-Large) | Tightened default to `#4B5563` (gray-600 equivalent, 7.6:1 contrast on white). Updated in BOTH `src/index.css` AND the fallback in `landi-token-colors.cjs` so the default cascades through Tailwind theme + raw `var()` consumers. Embedders can override per tenant. |

### NIT — DEFERRED (acceptable per reviewer)

| # | Finding | Plan |
|---|---|---|
| N1 | `border-gray-100/200/300` flatten to single `canvas-border` is information-loss in theory; in practice the variation was incidental authoring noise. No action. | None. |
| N2 | No `hover:bg-canvas-primary-tint` + `hover:bg-gray-100` collisions found. | None. |
| N3 | Visual regression / Playwright `toHaveScreenshot()` not yet captured. Reviewer accepts the mechanical-sweep + 51/51 unit-pass + dark-theme-fallback-trace as sufficient for F.7.2 alone, but flags it as a closeout requirement before F.7.3 closes M2.7 W2 entirely. | Tracked as a M2.7 follow-up alongside the `@open-wc/testing` harness (F.7.3). |

## Verification post-fix

- 51/51 vitest tests pass.
- Final grep for ALL grayscale utilities (`bg-gray-100|200|300|bg-white|hover:bg-gray-50|100|border-gray-100|200|300|text-gray-300..900`) in `src/canvas/`: **0 matches**.
- New `canvas-border` token alias emits in `dist/embed/agentable-canvas.css` (post-`npm run build:embed` clean).
- Dark-theme fallback chain traced clean across the 9 originally-flagged sites; CRITICAL + WARNING sites no longer leak brand.

## Status

**DONE.** All CRITICAL + actionable WARNING resolved same-iteration. NIT deferred per reviewer rationale.
